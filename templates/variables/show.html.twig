{% extends 'base.html.twig' %}

{% block title %}Variables{% endblock %}

{% block body %}
    <h1>Variables</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ variable.id }}</td>
            </tr>
            <tr>
                <th>Switch</th>
                <td>{{ variable.switch }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ variable.couleur }}</td>
            </tr>
            <tr>
                <th>Epaisseur</th>
                <td>{{ variable.epaisseur }}</td>
            </tr>
            <tr>
                <th>Trydefou</th>
                <td>{{ variable.trydefou }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('variables_index') }}">back to list</a>

    <a href="{{ path('variables_edit', {'id': variable.id}) }}">edit</a>

    {{ include('variables/_delete_form.html.twig') }}
{% endblock %}
